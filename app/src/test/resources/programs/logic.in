{
  -- Zero is false; every nonzero integer is true.
  write(0 && 0);
  write(1 && 0);
  write(0 && 1);
  write(2 && 3);
  write(0 !! 0);
  write(1 !! 0);
  write(0 !! (0 - 3));
  write(2 !! 3);
  write((1 < 2) && (0 !! 5));
}
