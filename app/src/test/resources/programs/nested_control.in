{
  read(rows);
  read(cols);
  count = 0;

  for (i = 0; i < rows; i += 1) {
    j = 0;
    while (j < cols) {
      if ((i + j) % 2 == 0)
        count += 1;
      else
        skip;
      j += 1;
    }
  }

  write(count);
}
