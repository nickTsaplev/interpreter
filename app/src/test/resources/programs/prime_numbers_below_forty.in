{
  n = 40;
  for (i = 2; i < n; i += 1) {
    is_prime = 1;

    for (j = 2; j * j <= i; j += 1)
      is_prime = is_prime && (i % j != 0);

    if (is_prime)
      write(i);
    else
      skip;
  }
}
