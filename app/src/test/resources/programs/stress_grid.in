{
  read(rows);
  read(cols);
  score = 0;
  evenCells = 0;
  diagonal = 0;

  for (i = 0; i < rows; i += 1) {
    j = 0;

    while (j < cols) {
      cell = (((i + 1) * (j + 2)) + ((i * j) % 7)) % 23;

      if ((i + j) % 3 == 0) {
        score += cell * 2;
        diagonal += 1;
      } elif (cell % 2 == 0) {
        score -= cell;
        evenCells += 1;
      } else {
        score += cell / 2;
      }

      if (((cell % 5 == 0) && (score != 0)) !!
          ((i == rows - 1) && (j == cols - 1)))
        write(score);
      else
        skip;

      j += 1;
    }
  }

  write(score);
  write(evenCells);
  write(diagonal);
}
